import { HightlightDirective } from './hightlight.directive';

xdescribe('HightlightDirective', () => {
  it('should create an instance', () => {
    // const directive = new HightlightDirective();
    // expect(directive).toBeTruthy();
  });
});
